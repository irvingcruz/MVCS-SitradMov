@model IEnumerable<BusinessEntity.BESitrad>
@{
    ViewBag.Title = "Resultado";
}
<p>&nbsp;</p>
<p>
    <h5>Resultado de la HT:</h5>
    <h6>@ViewBag.Tramite</h6>
</p>

<div class="font-table">
   
        @*@foreach (var item in Model)
        {
            var vDocumento = item.Documento.Replace("/", "|");
            <div class="row">
                <div class="col s1"><a href="@Url.Action("Detalle","Sitrad",new { IDTramite = item.IDTramite, Documento = vDocumento })"><i class="material-icons">info</i></a></div>
                <div class="col s11">@Html.DisplayFor(modelItem => item.Destinatario) | @Html.DisplayFor(modelItem => item.Destinatario)</div>
            </div>

        

        }*@

    <ul class="collapsible" data-collapsible="accordion">

        @foreach (var item in Model)
        {
            <li>
                <div class="collapsible-header"><i class="material-icons">info</i>@Html.DisplayFor(modelItem => item.Documento) | @Html.DisplayFor(modelItem => item.Destinatario) | @Html.DisplayFor(modelItem => item.Destinatario)</div>
                <div class="collapsible-body">
                    <div class="row">
                        <div class="col s12">
                            <ul class="tabs">
                                @foreach (var det in item.oTraza)
                                {
                                    var link = "#tb" + item.Nro + det.Nro;
                                    <li class="tab col s1"><a href="@link">@Html.DisplayFor(modelDet => det.Nro)</a> </li>
                                }                                
                            </ul>
                        </div>
                        @foreach (var det in item.oTraza)
                        {
                            var link = "tb" + item.Nro + det.Nro;
                            <div id="@link" class="col s12">
                                <p class="body-text">
                                    <span style="font-weight: bold">Envia-Area:</span>@Html.DisplayFor(modelDet => det.EnviaArea)<br />
                                    <span style="font-weight: bold">Envia-Fecha:</span>@Html.DisplayFor(modelDet => det.EnviaFecha)<br />
                                    <span style="font-weight: bold">Recibe-Area:</span>@Html.DisplayFor(modelDet => det.RecibeArea)<br />
                                    <span style="font-weight: bold">Recibe-Fecha:</span>@Html.DisplayFor(modelDet => det.RecibeFecha)
                                </p>
                            </div>
                        }                                               
                    </div>
                </div>
            </li>
        }

    </ul>

</div>

