@model IEnumerable<BusinessEntity.BESitrad>
@{
    ViewBag.Title = "Resultado";
}
<p>&nbsp;</p>
<p>
    <h5>Resultado de la HT:</h5>
    <h6>@ViewBag.Tramite</h6>
</p>

<div class="font-table">
   
        @*@foreach (var item in Model)
        {
            var vDocumento = item.Documento.Replace("/", "|");
            <div class="row">
                <div class="col s1"><a href="@Url.Action("Detalle","Sitrad",new { IDTramite = item.IDTramite, Documento = vDocumento })"><i class="material-icons">info</i></a></div>
                <div class="col s11">@Html.DisplayFor(modelItem => item.Destinatario) | @Html.DisplayFor(modelItem => item.Destinatario)</div>
            </div>

        

        }*@

    <ul class="collapsible" data-collapsible="accordion">

        @foreach (var item in Model)
        {
            <li>
                <div class="collapsible-header"><i class="material-icons">info</i>@Html.DisplayFor(modelItem => item.EnviaArea) | @Html.DisplayFor(modelItem => item.RecibeArea)</div>
                <div class="collapsible-body">
                    <div class="row">
                        <div class="col s12">
                            <p class="body-text">
                                <span style="font-weight: bold; text-transform: uppercase;">DOCUMENTO:</span>&nbsp;@Html.DisplayFor(modelItem => item.Documento)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">F. ENVIA:</span>&nbsp;@Html.DisplayFor(modelItem => item.EnviaFecha)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">U. ENVIA:</span>&nbsp;@Html.DisplayFor(modelItem => item.EnviaUser)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">F. RECIBE:</span>&nbsp;@Html.DisplayFor(modelItem => item.RecibeFecha)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">U. RECIBE:</span>&nbsp;@Html.DisplayFor(modelItem => item.RecibeUser)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">ESTADO:</span>&nbsp;@Html.DisplayFor(modelItem => item.Estado)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">DIAS:</span>&nbsp;@Html.DisplayFor(modelItem => item.Dias)<br />
                                <span style="font-weight: bold; text-transform: uppercase;">OBSERV.:</span>&nbsp;@Html.DisplayFor(modelItem => item.Observaciones)<br />
                            </p>
                        </div>                                             
                    </div>
                </div>
            </li>
        }

    </ul>

</div>

